#!/bin/sh

set -e

if [ "$1" = "remove" ]; then
	if [ -x /usr/sbin/a2dismod ]; then
	    a2dismod php5-cgi || true
	fi
    update-alternatives --remove php-cgi /usr/bin/php5-cgi
    update-alternatives --remove php-cgi-bin /usr/lib/cgi-bin/php5
fi

#DEBHELPER#

exit 0
